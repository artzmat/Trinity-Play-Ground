# personas/.deprecated

Historical persona files kept for reference after role consolidation. These
are no longer the active persona surface — see the parent `personas/`
directory for the current ones.

## tool-agent/

The fourth voice (Command-R on LM Studio slot :3 for Cline/tool work),
established 2026-06-07 and **absorbed into Center** the same day. Center
is now Command-R on slot :3, so the Tool Agent role (a separate persona
for tool-calling work) is redundant.

The files in `tool-agent/` are preserved here so the design history and
the original TNMP-Cline-Rules.md are recoverable. They are not loaded by
any active launcher or LM Studio slot.
